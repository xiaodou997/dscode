[package]
name = "dscode-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"
toml_edit = "=0.25.12"

[lints]
workspace = true
